---
- hosts: webservers

  # an encrypted ansible secret file containing the sudo password
  vars_files:
    - secret

  roles:
    - flask-api

  environment:
    DEBUG: "{{ lookup('ansible.builtin.env', 'DEBUG') }}"
